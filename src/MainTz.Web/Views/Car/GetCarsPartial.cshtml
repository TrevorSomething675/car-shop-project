@using MainTz.Web.ViewModels.CarViewModels
@model CarsViewModel

@foreach (var car in Model.CarsResponse)
{
    <div class="col-xxl-3 col-xl-4 col-md-6 col-sm-12 my-2">
        @await Html.PartialAsync("GetCarCardPartial", car)
    </div>
}